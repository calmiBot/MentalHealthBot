# Data Directory

This directory stores persistent data:
- SQLite database file (when using SQLite mode)
- Any other data files

**Note:** This directory is mounted as a volume in Docker to persist data between container restarts.
